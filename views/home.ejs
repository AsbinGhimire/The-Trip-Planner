<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shuba Yatra | Premium Trip Planner</title>
    <link rel="stylesheet" href="/main.css">
    <link rel="stylesheet" href="/navbar.css">
    <style>
        .hero {
            position: relative;
            padding: 8rem 0;
            background: linear-gradient(rgba(15, 23, 42, 0.7), rgba(15, 23, 42, 0.7)), 
                        url('https://images.unsplash.com/photo-1469854523086-cc02fe5d8800?ixlib=rb-4.0.3&auto=format&fit=crop&w=2021&q=80');
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
            color: var(--white);
            text-align: center;
            border-bottom-left-radius: 4rem;
            border-bottom-right-radius: 4rem;
            margin-bottom: -4rem;
            z-index: 1;
        }

        .hero-content {
            max-width: 800px;
            margin: 0 auto;
        }

        .hero h1 {
            font-size: 4rem;
            color: var(--white);
            margin-bottom: var(--space-md);
            animation: fadeInUp 0.8s ease-out;
        }

        .hero p {
            font-size: 1.25rem;
            opacity: 0.9;
            margin-bottom: var(--space-lg);
            animation: fadeInUp 1s ease-out;
        }

        .hero-btns {
            display: flex;
            justify-content: center;
            gap: var(--space-md);
            animation: fadeInUp 1.2s ease-out;
        }

        .btn-large {
            padding: 1rem 2.5rem;
            font-size: 1.125rem;
        }

        .section-header {
            text-align: center;
            margin-bottom: var(--space-xl);
            position: relative;
            z-index: 2;
        }

        .section-title {
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: var(--space-xs);
        }

        .section-subtitle {
            color: var(--text-secondary);
            font-size: 1.1rem;
        }

        .trips-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            gap: var(--space-lg);
            margin-top: var(--space-lg);
        }

        .trip-card {
            cursor: pointer;
        }

        .trip-image-wrapper {
            position: relative;
            height: 240px;
            overflow: hidden;
        }

        .trip-image-wrapper img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: var(--transition-slow);
        }

        .trip-card:hover .trip-image-wrapper img {
            transform: scale(1.1);
        }

        .trip-info {
            padding: var(--space-md);
        }

        .trip-name {
            font-size: 1.25rem;
            font-weight: 600;
            margin-bottom: var(--space-xs);
        }

        .features-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: var(--space-lg);
        }

        .feature-item {
            padding: var(--space-lg);
            text-align: center;
            background: var(--white);
            border-radius: var(--radius-lg);
            box-shadow: var(--shadow-sm);
            transition: var(--transition-base);
        }

        .feature-item:hover {
            transform: translateY(-5px);
            box-shadow: var(--shadow-md);
        }

        .feature-icon {
            font-size: 2.5rem;
            margin-bottom: var(--space-sm);
            display: block;
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @media (max-width: 768px) {
            .hero h1 { font-size: 2.5rem; }
            .features-grid { grid-template-columns: repeat(2, 1fr); }
            .hero-btns { flex-direction: column; align-items: center; }
        }
    </style>
</head>
<body>
    <%- include('./components/navbar') %>

    <header class="hero">
        <div class="container hero-content">
            <h1>Born To Have Adventure</h1>
            <p>Your premium companion for discovering new destinations, creating unforgettable memories, and exploring the world in style.</p>
            <div class="hero-btns">
                <button class="btn btn-primary btn-large" onclick="window.location.href='/plan'">Plan Your Trip</button>
                <button class="btn btn-secondary btn-large" onclick="window.location.href='/hotels'">View Hotels</button>
            </div>
        </div>
    </header>

    <main class="container section">
        <div class="section-header">
            <h2 class="section-title">MOST POPULAR TRIPS</h2>
            <p class="section-subtitle">Discover our curated list of exceptional destinations</p>
        </div>

        <div class="trips-grid">
            <% trips.forEach((trip) => { %>
                <article class="card-premium trip-card" onclick="window.location.href='/trip/<%- trip.id %>'">
                    <div class="trip-image-wrapper">
                        <img src="<%- trip.tripImages[0].imageUrl %>" alt="<%- trip.name %>">
                        <div class="glass" style="position: absolute; bottom: 0; left: 0; right: 0; padding: 1rem;">
                            <h3 style="margin: 0; color: var(--text-primary);"><%- trip.name %></h3>
                        </div>
                    </div>
                </article>
            <% }) %>
        </div>
    </main>

    <section class="section" style="background-color: #f1f5f9;">
        <div class="container">
            <div class="section-header">
                <h2 class="section-title">WHY SUBHA YATRA?</h2>
                <p class="section-subtitle">The standard for premium travel planning</p>
            </div>
            
            <div class="features-grid">
                <div class="feature-item">
                    <span class="feature-icon">💎</span>
                    <h3>Best Prices</h3>
                    <p style="color: var(--text-secondary); margin-top: 0.5rem;">Unmatched value for luxury travel.</p>
                </div>
                <div class="feature-item">
                    <span class="feature-icon">🌟</span>
                    <h3>Quality Service</h3>
                    <p style="color: var(--text-secondary); margin-top: 0.5rem;">Dedicated support for every journey.</p>
                </div>
                <div class="feature-item">
                    <span class="feature-icon">😊</span>
                    <h3>Satisfaction</h3>
                    <p style="color: var(--text-secondary); margin-top: 0.5rem;">Memories that last a lifetime.</p>
                </div>
                <div class="feature-item">
                    <span class="feature-icon">⚡</span>
                    <h3>Easy Booking</h3>
                    <p style="color: var(--text-secondary); margin-top: 0.5rem;">Seamless and intuitive experience.</p>
                </div>
            </div>
        </div>
    </section>

    <section class="container section">
        <div class="section-header">
            <h2 class="section-title">HOW SUBHA YATRA HELPS?</h2>
            <p class="section-subtitle">Simplified planning for extraordinary trips</p>
        </div>
        
        <div class="features-grid">
            <div class="feature-item card-premium">
                <span class="feature-icon">📝</span>
                <h3>Plan Your Trip</h3>
            </div>
            <div class="feature-item card-premium">
                <span class="feature-icon">📍</span>
                <h3>Find Destination</h3>
            </div>
            <div class="feature-item card-premium">
                <span class="feature-icon">🎟️</span>
                <h3>Book Tickets</h3>
            </div>
            <div class="feature-item card-premium">
                <span class="feature-icon">⛵</span>
                <h3>Enjoy Journey</h3>
            </div>
        </div>
    </section>

    <%- include('./components/footer') %>

    <script>
        // Simple search functionality if needed later
        function handleSearch(){
            const input = document.getElementById('inputSearch').value.toLowerCase();
            const cards = document.querySelectorAll('.trip-card');

            cards.forEach(card => {
                const title = card.querySelector('h3').textContent.toLowerCase();
                if(title.includes(input)) {
                    card.style.display = "block";
                } else {
                    card.style.display = "none";
                }
            });
        }
    </script>
</body>
</html>
