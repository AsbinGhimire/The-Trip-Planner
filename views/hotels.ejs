<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Browse Hotels | Shuba Yatra</title>
    <link rel="stylesheet" href="/main.css">
    <link rel="stylesheet" href="/navbar.css">
    <style>
        .hotels-hero {
            padding: 6rem 0 4rem;
            background: linear-gradient(rgba(15, 23, 42, 0.8), rgba(15, 23, 42, 0.8)),
                        url('https://images.unsplash.com/photo-1566073771259-6a8506099945?ixlib=rb-4.0.3&auto=format&fit=crop&w=2070&q=80');
            background-size: cover;
            background-position: center;
            color: var(--white);
            text-align: center;
        }

        .search-container {
            max-width: 700px;
            margin: -2rem auto 0;
            padding: 1.5rem;
            background: var(--white);
            border-radius: var(--radius-lg);
            box-shadow: var(--shadow-xl);
            display: flex;
            gap: 1rem;
            position: relative;
            z-index: 10;
        }

        .search-input {
            flex: 1;
            padding: 0.75rem 1.25rem;
            border: 1px solid #e2e8f0;
            border-radius: var(--radius-md);
            font-family: inherit;
        }

        .hotels-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
            gap: var(--space-lg);
            padding: var(--space-xl) 0;
        }

        .hotel-card {
            display: flex;
            flex-direction: column;
            cursor: pointer;
        }

        .hotel-image {
            height: 220px;
            overflow: hidden;
            position: relative;
        }

        .hotel-image img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: var(--transition-slow);
        }

        .hotel-card:hover .hotel-image img {
            transform: scale(1.05);
        }

        .hotel-content {
            padding: var(--space-md);
            flex: 1;
            display: flex;
            flex-direction: column;
        }

        .hotel-title {
            font-size: 1.25rem;
            font-weight: 700;
            margin-bottom: 0.5rem;
        }

        .hotel-address {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            color: var(--text-secondary);
            font-size: 0.9rem;
            margin-bottom: 1rem;
        }

        .hotel-badge {
            position: absolute;
            top: 1rem;
            right: 1rem;
            padding: 0.5rem 1rem;
            font-size: 0.8rem;
            font-weight: 600;
            color: var(--white);
            background: var(--primary);
            border-radius: var(--radius-full);
            box-shadow: var(--shadow-md);
        }
    </style>
</head>
<body>
    <%- include('./components/navbar') %>

    <header class="hotels-hero">
        <div class="container">
            <h1 style="font-size: 3rem; margin-bottom: 1rem;">Find Your Perfect Stay</h1>
            <p style="opacity: 0.9; font-size: 1.1rem;">Luxury hotels and cozy retreats curated just for you</p>
        </div>
    </header>

    <div class="container">
        <div class="search-container">
            <input type="text" class="search-input" id="inputSearch" placeholder="Where are you going?" oninput="handleSearch()">
            <button class="btn btn-primary" onclick="handleSearch()">Explore</button>
        </div>

        <section class="hotels-grid">
            <% datas.forEach((data) => { %>
                <article class="card-premium hotel-card" onclick="window.location.href='/hotel/<%- data.id %>'">
                    <div class="hotel-image">
                        <% if((data.hotelbooks && data.hotelbooks.length > 0) || (data.hotelreviews && data.hotelreviews.length > 0)) { %>
                            <img src="/<%- (data.hotelImages && data.hotelImages.length > 0) ? data.hotelImages[0].imageUrl : 'https://via.placeholder.com/400x300?text=No+Image' %>" alt="<%- data.hotel ?? data.hotelName %>">
                        <% } else { %>
                            <div style="height: 100%; display: flex; flex-direction: column; align-items: center; justify-content: center; background: #f1f5f9; color: var(--text-muted); padding: 2rem; text-align: center;">
                                <span style="font-size: 3rem; margin-bottom: 1rem;">🏨</span>
                                <p style="font-weight: 600; font-size: 0.9rem;">New to Shuba Yatra</p>
                                <p style="font-size: 0.8rem;">Booking Opening Soon</p>
                            </div>
                        <% } %>
                        <span class="hotel-badge">Premium</span>
                    </div>
                    <div class="hotel-content">
                        <h3 class="hotel-title"><%- data.hotel ?? data.hotelName %></h3>
                        <div class="hotel-address">
                            <span>📍</span>
                            <%- data.hotelAddress %>
                        </div>
                        <div style="margin-top: auto; display: flex; justify-content: space-between; align-items: center;">
                            <span style="color: var(--primary); font-weight: 700; font-size: 1.1rem;">Explore Details</span>
                            <span style="font-size: 1.2rem;">→</span>
                        </div>
                    </div>
                </article>
            <% }) %>
        </section>
    </div>

    <%- include('./components/footer') %>

    <script>
        function handleSearch(){
            const input = document.getElementById('inputSearch').value.toLowerCase();
            const cards = document.querySelectorAll('.hotel-card');

            cards.forEach(card => {
                const title = card.querySelector('.hotel-title').textContent.toLowerCase();
                const address = card.querySelector('.hotel-address').textContent.toLowerCase();

                if(title.includes(input) || address.includes(input)) {
                    card.style.display = "flex";
                } else {
                    card.style.display = "none";
                }
            });
        }
    </script>
</body>
</html>
