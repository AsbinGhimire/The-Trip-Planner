<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Plan Your Journey | Shuba Yatra</title>
    <link rel="stylesheet" href="/main.css">
    <link rel="stylesheet" href="/navbar.css">
    <style>
        .plan-hero {
            padding: 8rem 0 6rem;
            background: linear-gradient(rgba(15, 23, 42, 0.7), rgba(15, 23, 42, 0.7)),
                        url('https://images.unsplash.com/photo-1544735716-99ff26b65f6c?ixlib=rb-4.0.3&auto=format&fit=crop&w=2070&q=80');
            background-size: cover;
            background-position: center;
            color: var(--white);
            text-align: center;
            border-bottom-left-radius: 4rem;
            border-bottom-right-radius: 4rem;
        }

        .plan-form-container {
            max-width: 900px;
            margin: -4rem auto 4rem;
            padding: 2.5rem;
            background: var(--white);
            border-radius: var(--radius-xl);
            box-shadow: var(--shadow-xl);
            position: relative;
            z-index: 10;
        }

        .form-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1.5rem;
            margin-bottom: 2rem;
        }

        .input-group {
            display: flex;
            flex-direction: column;
            gap: 0.5rem;
        }

        .input-group label {
            font-weight: 700;
            font-size: 0.85rem;
            text-transform: uppercase;
            color: var(--text-secondary);
        }

        .input-group input {
            padding: 0.85rem 1rem;
            border: 1px solid #e2e8f0;
            border-radius: var(--radius-md);
            font-family: inherit;
        }

        .input-group input:focus {
            outline: none;
            border-color: var(--primary);
            box-shadow: 0 0 0 3px rgba(99, 102, 241, 0.1);
        }

        .checkbox-group {
            display: flex;
            align-items: center;
            gap: 0.75rem;
            margin-top: 1rem;
        }

        .checkbox-group input[type="checkbox"] {
            width: 1.25rem;
            height: 1.25rem;
            accent-color: var(--primary);
        }

        .section-title {
            text-align: center;
            margin: 4rem 0 3rem;
        }

        .section-title h2 {
            font-size: 2.5rem;
            margin-bottom: 0.5rem;
        }
    </style>
</head>
<body>
    <%- include('./components/navbar') %>

    <header class="plan-hero">
        <div class="container">
            <h1 style="font-size: 4rem;">Let's Begin The Journey</h1>
            <p style="font-size: 1.25rem; opacity: 0.9; margin-top: 1rem;">Map out your vision, we'll handle the rest.</p>
        </div>
    </header>

    <main class="container">
        <div class="plan-form-container">
            <form action="/plan" method="post">
                <div class="form-grid">
                    <div class="input-group">
                        <label>Departure</label>
                        <input type="text" name="from" placeholder="Current Location" required>
                    </div>
                    <div class="input-group">
                        <label>Destination</label>
                        <input type="text" name="to" placeholder="Search City/Place" required>
                    </div>
                </div>

                <% if(currentUser){ %>
                    <div class="form-grid">
                        <div class="input-group">
                            <label>Arrival Date</label>
                            <input type="date" name="startDate" required>
                        </div>
                        <div class="input-group">
                            <label>Return Date</label>
                            <input type="date" name="endDate">
                        </div>
                    </div>
                    <div class="checkbox-group">
                        <input type="checkbox" id="history" name="history" value="true">
                        <label for="history">Save this trip to my itinerary</label>
                    </div>
                <% } %>

                <div style="margin-top: 2.5rem; text-align: center;">
                    <button type="submit" class="btn btn-primary" style="padding: 1rem 4rem; font-size: 1.1rem;">Search Best Routes</button>
                    <% if(!currentUser) { %>
                        <p style="margin-top: 1rem; font-size: 0.9rem; color: var(--text-secondary);">
                            <a href="/login" style="color: var(--primary); font-weight: 600;">Sign in</a> to save plans and get personalized suggestions
                        </p>
                    <% } %>
                </div>
            </form>
        </div>

        <section class="section-title">
            <div class="feature-badge" style="margin-bottom: 1rem;">Inspiration</div>
            <h2>Top Gems of Nepal</h2>
            <p style="color: var(--text-secondary);">Discover the most breathtaking destinations curated by local experts</p>
        </section>

        <!-- Dynamic Content placeholder or featured section could go here -->
    </main>

    <%- include('./components/footer') %>
</body>
</html>
