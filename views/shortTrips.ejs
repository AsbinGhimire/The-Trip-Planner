<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Planning History | Shuba Yatra</title>
    <link rel="stylesheet" href="/main.css">
    <link rel="stylesheet" href="/navbar.css">
    <style>
        .history-hero {
            padding: 4rem 0;
            background: linear-gradient(135deg, var(--teal), #0f766e);
            color: var(--white);
            border-bottom-left-radius: 2rem;
            border-bottom-right-radius: 2rem;
            margin-bottom: 3rem;
        }

        .table-wrapper {
            background: var(--white);
            border-radius: var(--radius-lg);
            box-shadow: var(--shadow-md);
            overflow: hidden;
            margin-bottom: 3rem;
        }

        .premium-table {
            width: 100%;
            border-collapse: separate;
            border-spacing: 0;
        }

        .premium-table th {
            background: #f8fafc;
            padding: 1.25rem 1rem;
            font-weight: 700;
            text-transform: uppercase;
            font-size: 0.8rem;
            letter-spacing: 0.05em;
            color: var(--text-secondary);
            border-bottom: 1px solid #e2e8f0;
        }

        .premium-table td {
            padding: 1.25rem 1rem;
            border-bottom: 1px solid #e2e8f0;
            color: var(--text-primary);
        }

        .premium-table tr:hover {
            background: #f1f5f9;
        }

        .route-badge {
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            font-weight: 700;
            color: var(--text-primary);
        }

        .btn-icon {
            padding: 0.5rem;
            border-radius: var(--radius-sm);
            border: 1px solid #e2e8f0;
            background: var(--white);
            cursor: pointer;
            transition: var(--transition-base);
        }

        .btn-icon:hover {
            background: #f1f5f9;
            border-color: var(--primary);
            color: var(--primary);
        }
    </style>
</head>
<body>
    <%- include('./components/navbar') %>

    <header class="history-hero">
        <div class="container">
            <h1 style="font-size: 2.5rem; margin-bottom: 0.5rem;">Planning Journal</h1>
            <p style="opacity: 0.9;">A record of your past route searches and intended adventures</p>
        </div>
    </header>

    <main class="container">
        <div class="table-wrapper">
            <table class="premium-table">
                <thead>
                    <tr>
                        <th>Route ID</th>
                        <th>Journey Path</th>
                        <th>Start Date</th>
                        <th>End Date</th>
                        <th style="text-align: right;">Action</th>
                    </tr>
                </thead>
                <tbody>
                    <% if(datas && datas.length > 0) { %>
                        <% datas.forEach((data) => { %>
                            <tr>
                                <td><small style="color: var(--text-muted);">#ST-<%- data.id %></small></td>
                                <td>
                                    <div class="route-badge">
                                        <span><%- data.from %></span>
                                        <span style="opacity: 0.4;">→</span>
                                        <span><%- data.to %></span>
                                    </div>
                                </td>
                                <td><%- data.startDate || 'flexible' %></td>
                                <td><%- data.endDate || 'flexible' %></td>
                                <td style="text-align: right;">
                                    <button class="btn-icon" title="View Map" onclick="window.location.href='/map?from=<%- data.from %>&to=<%- data.to %>'">🗺️</button>
                                    <button class="btn-icon" title="Remove" onclick="if(confirm('Remove this entry?')) window.location.href='/deletest/<%- data.id %>'">🗑️</button>
                                </td>
                            </tr>
                        <% }) %>
                    <% } else { %>
                        <tr>
                            <td colspan="5" style="padding: 4rem; text-align: center; color: var(--text-muted);">
                                <div style="font-size: 3rem; margin-bottom: 1rem;">📍</div>
                                <p>Your planning history is empty.</p>
                                <a href="/plan" class="btn btn-primary" style="margin-top: 1rem;">Map a New Route</a>
                            </td>
                        </tr>
                    <% } %>
                </tbody>
            </table>
        </div>
    </main>

    <%- include('./components/footer') %>
</body>
</html>
