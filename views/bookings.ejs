<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Bookings | Shuba Yatra</title>
    <link rel="stylesheet" href="/main.css">
    <link rel="stylesheet" href="/navbar.css">
    <style>
        .bookings-hero {
            padding: 4rem 0;
            background: linear-gradient(135deg, var(--primary), var(--primary-hover));
            color: var(--white);
            border-bottom-left-radius: 2rem;
            border-bottom-right-radius: 2rem;
            margin-bottom: 3rem;
        }

        .table-wrapper {
            background: var(--white);
            border-radius: var(--radius-lg);
            box-shadow: var(--shadow-md);
            overflow: hidden;
            margin-bottom: 3rem;
        }

        .premium-table {
            width: 100%;
            border-collapse: separate;
            border-spacing: 0;
        }

        .premium-table th {
            background: #f8fafc;
            padding: 1.25rem 1rem;
            font-weight: 700;
            text-transform: uppercase;
            font-size: 0.8rem;
            letter-spacing: 0.05em;
            color: var(--text-secondary);
            border-bottom: 1px solid #e2e8f0;
        }

        .premium-table td {
            padding: 1.25rem 1rem;
            border-bottom: 1px solid #e2e8f0;
            color: var(--text-primary);
        }

        .premium-table tr:hover {
            background: #f1f5f9;
        }

        .status-badge {
            display: inline-block;
            padding: 0.35rem 0.75rem;
            border-radius: var(--radius-full);
            font-size: 0.8rem;
            font-weight: 600;
            text-transform: capitalize;
        }

        .status-paid { background: #dcfce7; color: #166534; }
        .status-pending { background: #fef9c3; color: #854d0e; }
        .status-unpaid { background: #fee2e2; color: #991b1b; }

        .btn-icon {
            padding: 0.5rem;
            border-radius: var(--radius-sm);
            border: 1px solid #e2e8f0;
            background: var(--white);
            cursor: pointer;
            transition: var(--transition-base);
        }

        .btn-icon:hover {
            background: #f1f5f9;
            border-color: var(--primary);
            color: var(--primary);
        }
    </style>
</head>
<body>
    <%- include('./components/navbar') %>

    <header class="bookings-hero">
        <div class="container">
            <h1 style="font-size: 2.5rem; margin-bottom: 0.5rem;">My Reservations</h1>
            <p style="opacity: 0.9;">Manage your upcoming stays and travel history</p>
        </div>
    </header>

    <main class="container">
        <div class="table-wrapper">
            <table class="premium-table">
                <thead>
                    <tr>
                        <th>Hotel</th>
                        <th>Location</th>
                        <th>Seats / Beds</th>
                        <th>Contact</th>
                        <th>Payment Status</th>
                        <th style="text-align: right;">Action</th>
                    </tr>
                </thead>
                <tbody>
                    <% if(data && data.length > 0) { %>
                        <% data.forEach((booking) => { %>
                            <tr>
                                <td>
                                    <div style="display: flex; align-items: center; gap: 1rem;">
                                        <% if(booking.hotel.hotelImages && booking.hotel.hotelImages.length > 0) { %>
                                            <img src="/<%- booking.hotel.hotelImages[0].imageUrl %>" alt="Hotel" style="width: 50px; height: 50px; border-radius: 8px; object-fit: cover;">
                                        <% } else { %>
                                            <div style="width: 50px; height: 50px; border-radius: 8px; background: #f1f5f9; display: flex; align-items: center; justify-content: center; font-size: 1.25rem;">🏨</div>
                                        <% } %>
                                        <div>
                                            <div style="font-weight: 700;"><%- booking.hotel.hotelName %></div>
                                            <small style="color: var(--text-muted);">Ref: #<%- booking.id %></small>
                                        </div>
                                    </div>
                                </td>
                                <td><%- booking.hotel.hotelAddress %></td>
                                <td><%- booking.totalSeats %></td>
                                <td><%- booking.phoneNo || 'N/A' %></td>
                                <td>
                                    <span class="status-badge status-<%- booking.paymentStatus.toLowerCase() %>">
                                        <%- booking.paymentStatus %>
                                    </span>
                                </td>
                                <td style="text-align: right;">
                                    <button class="btn-icon" title="Edit">✏️</button>
                                    <button class="btn-icon" title="Cancel" onclick="if(confirm('Are you sure you want to cancel this booking?')) window.location.href='/deletebooking/<%- booking.id %>'">🗑️</button>
                                </td>
                            </tr>
                        <% }) %>
                    <% } else { %>
                        <tr>
                            <td colspan="6" style="padding: 4rem; text-align: center; color: var(--text-muted);">
                                <div style="font-size: 3rem; margin-bottom: 1rem;">🧳</div>
                                <p>No bookings found. Time to plan your next adventure!</p>
                                <a href="/hotels" class="btn btn-primary" style="margin-top: 1rem;">Explore Hotels</a>
                            </td>
                        </tr>
                    <% } %>
                </tbody>
            </table>
        </div>
    </main>

    <%- include('./components/footer') %>
</body>
</html>
