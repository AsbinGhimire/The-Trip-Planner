<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Hotel | Admin Console</title>
    <link rel="stylesheet" href="/main.css">
    <link rel="stylesheet" href="/navbar.css">
    <style>
        .admin-wrapper {
            padding: 4rem 0;
            background: #f1f5f9;
            min-height: calc(100vh - 150px);
        }

        .form-card {
            max-width: 800px;
            margin: 0 auto;
            padding: 3rem;
            background: var(--white);
        }

        .form-header {
            margin-bottom: 2.5rem;
            padding-bottom: 1.5rem;
            border-bottom: 1px solid #e2e8f0;
        }

        .form-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 2rem;
        }

        .form-group {
            margin-bottom: 1.5rem;
        }

        .form-label {
            display: block;
            font-weight: 700;
            font-size: 0.85rem;
            text-transform: uppercase;
            color: var(--text-secondary);
            margin-bottom: 0.5rem;
        }

        .form-control {
            width: 100%;
            padding: 0.75rem 1rem;
            border: 1px solid #cbd5e1;
            border-radius: var(--radius-md);
            font-family: inherit;
        }

        .form-control:focus {
            outline: none;
            border-color: var(--primary);
            box-shadow: 0 0 0 3px rgba(99, 102, 241, 0.1);
        }

        textarea.form-control {
            min-height: 150px;
            resize: vertical;
        }

        .file-upload-box {
            border: 2px dashed #cbd5e1;
            padding: 2rem;
            text-align: center;
            border-radius: var(--radius-md);
            background: #f8fafc;
            cursor: pointer;
            transition: var(--transition-base);
        }

        .file-upload-box:hover {
            border-color: var(--primary);
            background: #f1f5f9;
        }
    </style>
</head>
<body>
    <%- include('../components/navbar') %>

    <main class="admin-wrapper">
        <div class="container">
            <div class="card-premium form-card">
                <div class="form-header">
                    <span class="feature-badge">Admin Resource</span>
                    <h1 style="font-size: 2rem; margin-top: 0.5rem;">Register New Hotel</h1>
                </div>

                <% if(message && message.length > 0) { %>
                    <div style="background: #fef2f2; color: #dc2626; padding: 1rem; border-radius: var(--radius-sm); margin-bottom: 2rem;">
                        <%- message %>
                    </div>
                <% } %>

                <form action="/admin/hotel" method="post" enctype="multipart/form-data">
                    <div class="form-grid">
                        <div class="form-group">
                            <label class="form-label">Hotel Name</label>
                            <input type="text" name="hotelName" class="form-control" placeholder="Grand Royal Resort" required>
                        </div>
                        <div class="form-group">
                            <label class="form-label">Price per Night (Rs.)</label>
                            <input type="number" name="hotelPrice" class="form-control" placeholder="4500" required>
                        </div>
                    </div>

                    <div class="form-group">
                        <label class="form-label">Hotel Address</label>
                        <input type="text" name="hotelAddress" class="form-control" placeholder="Lakeside, Pokhara, Nepal" required>
                    </div>

                    <div class="form-group">
                        <label class="form-label">Property Description</label>
                        <textarea name="hotelDescription" class="form-control" placeholder="Describe the hotel's amenities, rooms, and unique selling points..."></textarea>
                    </div>

                    <div class="form-group">
                        <label class="form-label">Gallery Images</label>
                        <div class="file-upload-box" onclick="document.getElementById('images').click()">
                            <div style="font-size: 2rem; margin-bottom: 0.5rem;">📁</div>
                            <p style="margin: 0; font-weight: 500;">Click to upload photos or drag and drop</p>
                            <small style="color: var(--text-muted);">Multiple files supported (Max 5MB each)</small>
                            <input type="file" id="images" name="images" required multiple style="display: none;">
                        </div>
                    </div>

                    <div style="margin-top: 3rem; display: flex; gap: 1rem; justify-content: flex-end;">
                        <button type="button" class="btn btn-secondary" onclick="history.back()">Cancel</button>
                        <button type="submit" class="btn btn-primary" style="padding-left: 3rem; padding-right: 3rem;">Create Hotel Listing</button>
                    </div>
                </form>
            </div>
        </div>
    </main>

    <%- include('../components/footer') %>
</body>
</html>
